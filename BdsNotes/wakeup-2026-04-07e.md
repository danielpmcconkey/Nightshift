# Wakeup — 2026-04-07e

## What happened this session

### v0.1.0 — idle polling + version stamping + publish pipeline

Committed all outstanding dirty work from session 07d (csproj, Program.cs version logging, publish.sh, wakeup files, .gitignore for .idea/ and .serena/). Added idle-poll behavior: engine sleeps 60s when queue is empty instead of exiting, controlled by engine_enabled flag.

Tagged `v0.1.0`, built release binary. Found double-v in version stamp (`vv0.1.0`).

### v0.1.1 — auto-activate unblocked cards

Engine was starting but not picking up P058 because no `task_queue` row existed for it. Root cause: when the old engine exited after P057 completed, it never enqueued the first step for newly-unblocked cards 6/7/8.

Fix: `TaskQueueRepository.ActivateUnblockedCards()` — finds queued cards with all dependencies complete and no pending/claimed tasks, enqueues their first workflow step. Called from the idle-poll branch in MainLoop, before the 60s sleep. If it activates any cards, loops back immediately to claim.

Also fixed the double-v: `publish.sh` now strips leading `v` from git tag before building version string.

Tagged `v0.1.1`, built release binary. Engine started, immediately activated card 6 (P058) and began processing.

**Commits:** `98ea4a1` (v0.1.0), `7ff01b0` (v0.1.1)

## What needs to happen next

1. **Monitor P058/P059/P061** — engine is running, P058 in progress
2. **After Epic L completes** — run GAAP audit crew against full LeoBloom codebase
3. **LeoBloom backlog index** — needs update (P038, P022, P035, P056, P057 all done)
4. **Foreman decision logging** — still outstanding from 07b
5. **Dogfood scaffolding** — register Nightshift as its own project
6. **Publish v0.1.0 tag to GitHub** — tags are local only

## Current state

### Branch: main
- HEAD: `7ff01b0` feat: auto-activate unblocked cards (v0.1.1)
- Working tree: this wakeup file only
- Tags: `v0.1.0` (98ea4a1), `v0.1.1` (7ff01b0)

### Engine
- Running in Dan's terminal: `dotnet /opt/nightshift/Nightshift.Engine.dll`
- Version: `0.1.1+7ff01b0`
- Card 6 (P058): in_progress at rte_setup
- Cards 7, 8 (P059, P061): queued, will auto-activate when card 6 completes

### Database: nightshift
- engine_enabled: true
- Cards 1–5: complete
- Card 6: in_progress
- Cards 7–8: queued, blocked on card 6

## Environment
- C# / .NET 10: `/usr/lib/dotnet` (on PATH)
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `nightshift`, database `nightshift`
- `NIGHTSHIFT_DB_PASSWORD` env var set
- Release binary: `/opt/nightshift/` (v0.1.1+7ff01b0)
- Run with: `dotnet /opt/nightshift/Nightshift.Engine.dll`
- DOTNET_ROOT issue: native exe needs `DOTNET_ROOT=/usr/lib/dotnet`, or just use `dotnet <dll>`
