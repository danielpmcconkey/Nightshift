# Wakeup — 2026-04-07g

## What happened this session

### Blueprint edits — test responsibility clarification

Edited 4 agent blueprints to fix a gap where nobody was reliably running the full LeoBloom test suite:

- **Builder** — scoped tests only during development. Full suite is not their job.
- **QE** — hard gate on full unfiltered `dotnet test`. Must save `test-results.md` artifact with commit hash. Any failure = card fails.
- **Reviewer** — checks QE's artifact exists and matches HEAD. Does not re-run tests.
- **Governor** — evidence auditor. Verifies artifact, rejects if missing or stale.

Committed in `0ac5024`.

### 12 new LeoBloom cards queued

Hobson's GAAP audit produced backlog items 062–072. BD wrote 073 (connection injection / test isolation). All inserted into the Nightshift DB:

| Card | Project | Priority | Blocked By |
|------|---------|----------|------------|
| 9 | P073 — Connection Injection | 1 | — |
| 10 | P062 — Normal Balance Consolidation | 1 | 9 |
| 11 | P068 — Fiscal Period Overlap Prevention | 2 | 9 |
| 12 | P069 — Account CRUD Behavioral Specs | 2 | 9 |
| 13 | P067 — Portfolio Validation Gaps | 2 | 9 |
| 14 | P063 — Portfolio Delete Restriction Tests | 2 | 9 |
| 15 | P064 — Balance Sheet Equation Verification | 2 | 9 |
| 16 | P065 — Balance Projection Filter Tests | 2 | 9 |
| 17 | P066 — Transfer Atomicity Tests | 2 | 9 |
| 18 | P070 — Missing Portfolio CLI Commands | 3 | 9 |
| 19 | P071 — CLI parseDate Consolidation | 3 | 9 |
| 20 | P072 — Housekeeping Batch | 3 | 9 |

### Engine restarted

v0.1.1 running in Dan's terminal. Picked up P073 immediately (rte_setup step).

### Retroactive BDD specs committed

4 feature files from the dogfooding discussion (CardLifecycle, WorkflowEngine, EngineLifecycle, TaskQueue) committed alongside blueprints. Still awaiting Dan's review before they become authoritative.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work (if it exists).

1. **P073 is running.** Monitor progress. This is the biggest card Nightshift has attempted — refactors every service function in LeoBloom.
2. **After P073:** 11 cards auto-unblock via v0.1.1 activation logic.
3. **Known issue:** LeoBloom test suite has intermittent parallel failures (~1 in 3-4 runs). P073 fixes this. Until then, QE's new hard gate on full suite may cause some cards to bounce through the review loop. That's correct behavior — the flakiness is real.
4. **Dogfooding specs** — Dan needs to review the 4 feature files before steps 2–5 of the dogfooding plan proceed.
5. **Blueprint sourcing** — blueprints are read from disk at runtime (`File.ReadAllText`). Ideally should be copied to bin dir at publish time so edits don't affect a running engine. Not urgent, noted for later.
6. **Timeout strategy needs rethinking (HIGH PRIORITY).** P073 builder timed out twice at 30 minutes. Bumped `DefaultTimeoutSeconds` to 5400 (90 min) as a band-aid. The real problem: a static timeout is always wrong — too short kills productive work, too long babysits dead agents. The correct solution is **progress detection**: if an agent hasn't produced output or made a tool call in N minutes, it's stuck and should be killed. If it's actively writing files, let it run. This is a Nightshift engine feature, not a config knob. Dan wants this prioritized — it'll bite us on every large card until it's fixed.

## Current state

### Branch: main
- HEAD: `0ac5024` chore: clarify test responsibility across agent blueprints
- Working tree: clean

### Engine
- Running: `dotnet /opt/nightshift/Nightshift.Engine.dll` (v0.1.1+7ff01b0)
- **Note:** Engine binary is v0.1.1 but blueprints on disk are newer than the binary. This is fine — blueprints are read from `/workspace/Nightshift/blueprints/`, not from the binary.
- Card 9 (P073): in_progress
- Cards 10–20: queued, blocked on 9

## Environment
- C# / .NET 10: `/usr/lib/dotnet` (on PATH)
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `nightshift`, database `nightshift`
- `NIGHTSHIFT_DB_PASSWORD` env var set
- Release binary: `/opt/nightshift/` (v0.1.1+7ff01b0)

## Copy-paste prompt for next session

```
read BdsNotes/wakeup-2026-04-07g.md in the Nightshift repo and report back
```
