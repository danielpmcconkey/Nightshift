# Wakeup — 2026-04-07f

## What happened this session

### v0.1.0 and v0.1.1 releases

See wakeup-2026-04-07e.md for full details. Short version:
- v0.1.0: idle polling (60s sleep instead of exit), version stamping, publish pipeline
- v0.1.1: auto-activate unblocked cards (fixes engine not picking up P058 after P057 completed), double-v fix in version stamp

Engine is running v0.1.1 in Dan's terminal, currently processing LeoBloom Epic L (P058 → P059 → P061).

### Retroactive BDD specs — draft complete, awaiting Dan's review

Four agents crawled the codebase and inferred business intent as Gherkin feature files:

| File | Scenarios | Domain |
|---|---|---|
| `Specs/CardLifecycle.feature` | 27 | Status transitions, dependencies, activation, priority |
| `Specs/WorkflowEngine.feature` | 33 | Step sequencing, outcomes, review gates, foreman, validation |
| `Specs/EngineLifecycle.feature` | 42 | Startup, shutdown, idle polling, agent invocation, security |
| `Specs/TaskQueue.feature` | 27 | Claiming, concurrency, completion, failure, orphan recovery |

Known issues:
- **Overlap between files.** Agents independently covered orphan recovery, activation, and claiming from different angles. Needs dedup — Dan decides which file owns what.
- **Inferred, not authored.** These capture what the code does, not necessarily what Dan intended. He's reviewing them to add/remove/correct before they become authoritative specs.

### Dogfooding plan — discussed, not started

Dan wants Nightshift to build Nightshift. The plan (from this session's discussion):
1. Retroactive Gherkins from code inference (done — specs in review)
2. Split into CLI project + underlying lib project
3. Create xUnit test project
4. Write xUnit tests matching the Gherkins
5. Add cards for a UI (boss demo — Jira → autonomous build)

Steps 2–5 are blocked on Dan approving the specs from step 1.

### LeoBloom documentation — killed

Discussed building proofmark-style docs for LeoBloom. Decided against it — agents have GAAP knowledge baked in, specs + ADRs already cover LeoBloom-specific decisions. Will use periodic GAAP audits (Hobson's audit crew) instead of static docs.

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work (if it exists — Nightshift doesn't have one yet).

1. **Dan reviews the 4 feature files.** Nothing moves until he's happy with them.
2. **After spec approval:** split into CLI + lib projects, create xUnit test project
3. **After tests:** write backlog cards for UI work
4. **LeoBloom:** after Epic L finishes (P058/P059/P061), run GAAP audit crew
5. **Commit specs + wakeups** — currently uncommitted

## Current state

### Branch: main
- HEAD: `7ff01b0` feat: auto-activate unblocked cards (v0.1.1)
- Working tree: dirty — `Specs/` (4 feature files), `BdsNotes/wakeup-2026-04-07e.md`, this file
- Tags: `v0.1.0` (98ea4a1), `v0.1.1` (7ff01b0)

### Engine
- Running: `dotnet /opt/nightshift/Nightshift.Engine.dll` (v0.1.1+7ff01b0)
- Card 6 (P058): in_progress
- Cards 7, 8 (P059, P061): queued, blocked on card 6

### Database: nightshift
- engine_enabled: true
- Cards 1–5: complete
- Card 6: in_progress
- Cards 7–8: queued

## Environment
- C# / .NET 10: `/usr/lib/dotnet` (on PATH)
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `nightshift`, database `nightshift`
- `NIGHTSHIFT_DB_PASSWORD` env var set
- Release binary: `/opt/nightshift/` (v0.1.1+7ff01b0)
- DOTNET_ROOT issue: native exe needs `DOTNET_ROOT=/usr/lib/dotnet`, or just use `dotnet <dll>`

## Copy-paste prompt for next session

```
read BdsNotes/wakeup-2026-04-07f.md in the Nightshift repo and report back
```
