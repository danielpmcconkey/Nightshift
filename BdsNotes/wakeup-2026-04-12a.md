# Wakeup — 2026-04-12a

## What happened this session

### OutcomeParser bug fix
Gherkin writer agent wrote `notes` as a JSON array instead of a string.
`OutcomeParser.Parse` called `GetString()` on it and threw
`InvalidOperationException`. Added `CoerceToString()` helper that handles
string, array, null, and other JSON types gracefully. Same treatment applied
to `reason` and `inject_context` fields.

File: `src/Nightshift.Engine/Agents/OutcomeParser.cs`

### Version bump to 0.1.2
Bumped `<Version>` in csproj. Standing rule: bump on every release build.

### Three LeoBloom cards completed
- Card 25 (P079 — Irregular Cadence): complete
- Card 26 (P080 — Reporting Data Extracts): complete
- Card 27 (P035 — Orphaned Posting Detection): complete

Queue is empty.

### QE timeout on card 27
QE agent hung for ~50 minutes on P035. Engine was still running but agent
never returned. Killed and restarted engine; QE completed in 14 seconds on
retry. Root cause unknown — likely an agent-side hang, not an engine bug.
The progress-based timeout (N001 on the Nightshift backlog) would have
caught this.

### PO blueprint rewrite
Rewrote `blueprints/po.md` to add business intent validation at both gates
and mandatory written artifacts (plan-review.md, signoff.md). The new PO
produced quality output on P080 — actual business judgment, not rubber stamps.

### Skills created
- `/db:nightshift` — query the Nightshift DB from BD's Claude
- `/db:leobloom_dev` — query the LeoBloom dev DB from BD's Claude

## Current state

### Branch: main
- HEAD: `0ac5024` chore: clarify test responsibility across agent blueprints
- **Uncommitted:** OutcomeParser fix, version bump to 0.1.2, this wakeup,
  BACKLOG.md (from previous session)
- Engine binary: v0.1.2 at `/opt/nightshift/`

### Queue
Empty. All cards complete (27 total).

## What needs to happen next

Read `BdsNotes/workflow.md` before starting work (if it exists).

1. **Commit uncommitted files** — OutcomeParser fix, version bump, wakeup,
   BACKLOG.md.
2. **New cards incoming** — Dan will enqueue P038, P022, and a new item from
   Hobson after BD finishes the requirements pass.
3. **N001 (progress-based timeout)** — high priority engine feature. The QE
   hang on card 27 is exactly the scenario this fixes. Still on the backlog.
4. **N004 (recurring workflows)** — design conversation with Dan pending.

## Environment
- C# / .NET 10: `/usr/lib/dotnet` (on PATH)
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `nightshift`, database `nightshift`
- `NIGHTSHIFT_DB_PASSWORD` env var set
- Release binary: `/opt/nightshift/` (v0.1.2)

## Copy-paste prompt for next session

```
read BdsNotes/wakeup-2026-04-12a.md in the Nightshift repo and report back
```
