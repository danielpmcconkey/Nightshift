# Wakeup — 2026-04-07d

## What happened this session

### File-based agent communication — confirmed working

Ported the Ogre interstitial file pattern (session 07c). P056 was the canary — it had been blocked by the markdown fence parsing bug. After the fix, P056 ran through builder_response → reviewer → governor → po_signoff → rte_merge → COMPLETE with zero parse failures. The fence problem is dead.

**Commits:** `d921139` (file-based comms), `4311ef0` (card dependencies)

### Card dependencies

Added `card_dependency` junction table (many-to-many blocking, not hierarchy). Claim query skips cards with incomplete dependencies. Tested live — P057 was correctly held until enqueued, and P058/P059/P061 are correctly blocked waiting on upstream cards.

Design decisions:
- Dependencies are blocking relationships, not parent-child. Epics (containment) will be a separate concept later.
- Card metadata (title, description, priority, dependencies) is immutable once `status != 'queued'`. Not DB-enforced — will be enforced at management surface layer.

### Investment portfolio cards enqueued

Hobson pushed P057-P061 backlog specs to LeoBloom. P060 (data migration) is Hobson's responsibility — runs on host, one-time ETL from PersonalFinance DB that BD can't see.

Cards enqueued with dependencies:
```
P057 (schema, no deps) → P058 (domain+repo) → P059 (CLI commands)
                                              → P061 (allocation reporting)
```

P056 completed. P057 is in progress (at QE as of this writing). Engine is running in Dan's separate terminal window.

### Publish pipeline and version tagging

Built release deployment workflow:
- `publish.sh` — builds release binary to `/opt/nightshift/`, stamps with git tag or `0.0.0-dev+<sha>`
- Version logged at startup: `Nightshift engine v<version>`
- csproj has `Version` property, `InformationalVersion` set at publish time
- `/opt/nightshift/` owned by sandbox user, binary runs with `dotnet Nightshift.Engine.dll`

**Not yet committed:** csproj changes, Program.cs version logging, publish.sh, this wakeup, previous wakeup (07c).

### Dogfooding discussion (not started)

Dan wants Nightshift to build Nightshift. Plan:
1. Publish release binary to `/opt/nightshift/`
2. Run release binary from separate terminal
3. Engine agents modify source in `/workspace/Nightshift/`, build debug, run tests
4. Manual release cycle: stop binary → `./publish.sh` → restart

Dan has "plans" for the dogfooding scaffolding but pivoted before detailing them.

## What needs to happen next

1. **Commit uncommitted work** — csproj, Program.cs, publish.sh, wakeup files
2. **Wait for P057 pipeline to finish** — engine is running, P057 at QE
3. **Publish v0.1.0 release** — tag, publish, verify. First official release.
4. **Dogfood scaffolding** — register Nightshift as a project in its own DB, create workflow, write blueprints/addenda. Dan has plans here.
5. **Foreman decision logging** — still outstanding from 07b. Every invocation logged, not just escalations.
6. **Morning brief query** — surface foreman decisions, test deltas, dependency chain stalls
7. **Review P022, P035** — still haven't been independently checked (P038 reviewed in 07b, P056 confirmed working)

## Current state

### Branch: main
- HEAD: `4311ef0` feat: card dependencies
- Working tree: dirty — csproj, Program.cs, publish.sh, two wakeup files
- Up to date with origin/main (uncommitted work is local only)

### Database: nightshift
- engine_enabled: true
- Engine running in Dan's terminal (PID unknown to BD)
- Card 4 (P056): complete
- Card 5 (P057): in_progress at QE
- Cards 6-8 (P058, P059, P061): queued, blocked on dependencies
- card_dependency table: 3 rows (6→5, 7→6, 8→6)

### Published binary
- Location: `/opt/nightshift/`
- Version: `0.0.0-dev+4311ef0` (pre-release, built from uncommitted state)

## Environment
- C# / .NET 10: `/usr/lib/dotnet` (on PATH)
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `nightshift`, database `nightshift`
- `NIGHTSHIFT_DB_PASSWORD` env var set by Hobson
- Claude CLI: `/usr/bin/claude` (on PATH)
- Release binary: `cd /opt/nightshift && dotnet Nightshift.Engine.dll`

## Copy-paste prompt for next session

```
read BdsNotes/wakeup-2026-04-07d.md in the Nightshift repo and report back
```
