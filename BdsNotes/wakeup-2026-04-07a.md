# Wakeup — 2026-04-07a

## What happened this session

### Engine fixes and improvements
1. **Connection string refactor** — Extracted monolithic `NIGHTSHIFT_CONNECTION_STRING` env var into `AddNightshiftDatabase()` extension method. Password comes from `NIGHTSHIFT_DB_PASSWORD`, non-secret config lives in `appsettings.json`.
2. **Renamed `clutch_engaged` → `engine_enabled`** — DB column, repo, EngineWorker, StepHandler, README, schema SQL. The clutch metaphor was ambiguous (driver vs engine perspective).
3. **Task ID tracking bug fix** — Inner loop created `TaskQueueItem` with `Id=0` after advancing steps. `Complete(0)` updated zero rows, leaving stale pending rows that violated `ix_task_queue_one_active`. Fix: `Enqueue` returns the real task ID, inner loop uses it.
4. **Foreman response parsing** — Foreman wrapped JSON in markdown fences, parser choked. Initially wrote a fence-stripping hack, then Dan correctly pointed out this was making the parser more permissive instead of enforcing the contract. Reverted hack, tightened the blueprint prompt instead. Response format now explicitly prohibits fences and explains `outcome` is parsed as an enum.

### Foreman blueprint (new)
- Wrote the full `foreman-jurisdiction.md` blueprint with Dan's input
- **BDD spec authority model:** load-bearing (merged) specs are inviolable; card (draft) specs are proposals subject to evaluation
- **Decision order:** load-bearing spec violation → fix code; card spec mismatch → check domain rules (GAAP if applicable) then SDLC best practice; overreaching card spec → escalate; weak card spec → retry at Gherkin Writer
- **Generic framework:** SDLC best practice or escalate to Dan
- **DSWF addendum loading:** Fixed `InvokeForeman` to actually load project addenda (was missing)

### LeoBloom foreman addendum (new)
- `DSWF/foreman-jurisdiction.md` in LeoBloom repo
- GAAP first, then SDLC, then escalate

### First engine run — 4 LeoBloom cards queued
- Registered LeoBloom as project 1 (workflow: bdd_pipeline, addenda: DSWF)
- Queued P038 (pri 1), P022 (pri 2), P035 (pri 2), P056 (pri 4)
- **P038 (CLI Obligation Commands): COMPLETE.** Full pipeline, clean. Reviewed independently — spec coverage 100%, 865 tests passing (up from 779), 68 new tests, all behavioral. Pattern compliance with P037/P041 confirmed.
- **P022 (Balance Projection): BLOCKED at reviewer.** Foreman made correct RETRY call (date-brittle tests + in-memory transfer filtering) but response had markdown fences, parser failed, auto-escalated. Needs requeue at reviewer after restart.
- **P035 (Orphaned Posting Detection): In progress at reviewer when engine was disabled.** Restart script is waiting for it to finish, then will rebuild and restart.
- **P056: Still queued.**

### Restart script running
- `/tmp/nightshift-restart.sh` is running detached (PID 95873)
- Polls for engine exit every 30s, then: rebuild, requeue P022 at reviewer (clear blocker), re-enable, restart
- Logs: `/tmp/nightshift-restart.log`
- Engine output: `/tmp/nightshift-restart.log` (appended after restart)

### Commits
- `86e3f61` refactor: extract connection string, rename clutch_engaged to engine_enabled
- `a3d813b` fix: task ID tracking in inner loop, write foreman-jurisdiction blueprint
- `229869a` feat: foreman blueprint with BDD spec authority, DSWF addendum loading
- `a394537` fix: tighten foreman response format, revert markdown fence stripping
- `0dec707` fix: Enqueue returns task ID for inner loop tracking

## What needs to happen next

1. **Check restart script results** — Did it work? Is the engine running? Check `/tmp/nightshift-restart.log`.
2. **Check P022 status** — Did the requeue + retry work? Did the foreman parse correctly this time?
3. **Check P035 and P056** — Did they complete or block?
4. **Morning brief items Dan wants:**
   - Foreman decision logging (code change — log every invocation, not just escalations)
   - Morning brief query to surface foreman decisions, test count deltas
5. **Push to origin** when Dan's ready
6. **LeoBloom repo state:** on branch `feat/p038-cli-obligation-commands` (engine's RTE created it). The `.gitignore` fix and foreman addendum landed on this branch. Main is at `1f3eed2`.

## Current state

### Branch: main
- HEAD: `0dec707` fix: Enqueue returns task ID for inner loop tracking
- Working tree: clean (`.idea/` untracked)
- 8 commits ahead of origin/main

### Database: nightshift
- engine_enabled: false (restart script will set true)
- Card 1 (P038): complete
- Card 2 (P022): blocked at reviewer (restart script will requeue)
- Card 3 (P035): in_progress at reviewer (engine processing, will exit after step)
- Card 4 (P056): queued

## Environment
- C# / .NET 10: `/usr/lib/dotnet` (on PATH)
- Git: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`
- PostgreSQL: `172.18.0.1:5432`, user `nightshift`, database `nightshift`
- `NIGHTSHIFT_DB_PASSWORD` env var set by Hobson
- Claude CLI: `/usr/bin/claude` (on PATH)
- Restart script: `/tmp/nightshift-restart.sh` (running)
- Engine logs: `/tmp/nightshift-restart.log`

## Copy-paste prompt for next session

```
read BdsNotes/wakeup-2026-04-07a.md in the Nightshift repo
```
